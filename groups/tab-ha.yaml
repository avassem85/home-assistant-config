ha_system:
  view: yes
  entities:
    - group.raspberry
    - group.home_assistant
  icon: mdi:wrench

raspberry:
  name: Raspberry Pi
  entities:
    - sensor.cpu_used
    - sensor.ram_free
    - sensor.disk_free_
    - sensor.last_boot
    - sensor.since_last_boot_templated

home_assistant:
  name: Raspberry Pi
  entities:
